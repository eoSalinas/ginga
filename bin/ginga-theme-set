#!/bin/bash

if [[ -z $1 && $1 != "CNCLD" ]]; then
  echo "Usage: ginga-theme-set <theme-name>"
  exit 1
fi

THEMES_DIR="$HOME/.config/ginga/themes/"
CURRENT_THEME_DIR="$HOME/.config/ginga/current/theme"

THEME_NAME=$(echo "$1" | sed -E 's/<[^>]+>//g' | tr '[:upper:]' '[:lower:]' | tr ' ' '-')
THEME_PATH="$THEMES_DIR/$THEME_NAME"

# Check if the theme entered exists
if [[ ! -d "$THEME_PATH" ]]; then
  echo "Theme '$THEME_NAME' does not exist in $THEMES_DIR"
  exit 1
fi

# Update theme symlinks
ln -nsf "$THEME_PATH" "$CURRENT_THEME_DIR"

# Change background with theme
ginga-theme-bg-next

# Restart components to apply new theme
if pgrep -x waybar >/dev/null; then
  ginga-restart-waybar
fi
ginga-restart-swayosd
ginga-restart-terminal
hyprctl reload
pkill -SIGUSR2 btop
makoctl reload

# Change gnome, browser, vscode, cursor themes
ginga-theme-set-gnome
ginga-theme-set-browser

# Call hook on theme set
ginga-hook theme-set "$THEME_NAME"
